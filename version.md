############# v0.1
-- 跑通一个产品(本地) v0.1 -> 原java项目，转成go语言, 上传, 备份
1. 把原项目: https://github.com/tiaodan/study-music-server-java.git


############# v0.2
-- 跑通一个产品(本地) v0.2 -> 把下载的歌曲, 存sql，归整到HDD(硬盘)

目的：把下载的歌曲，根据 歌手-专辑，把名字、歌词名字重新格式化，规整进数据库，并移动到HDD
仅后端实现相关接口
需求拆分：
1. 名字重新格式化。
	- 实现：api指定 歌手-专辑路径，自动遍历文件(mp3/wav/layic)名字,重新格式化(多作者-歌名.文件类型)
2. 移动到HDD
	- 实现：api指定路径，from - to ,根据路径，移动；移动后，查询是否移动成功
3. 规整进数据库
	- 实现：api指定路径，path:xx, 自动遍历文件(mp3/wav/layic)名字,
		-- 插入作者。如果有新作者，插入数据库
		-- 把歌曲名，插入song表
		-- 关联song 和 singer关系

重新设计数据库 （保证数据正确性），
sing/song表应该是有关联关系的，关联时删，不能删，修改时，同时改

############# v0.2.1
优化：
1. 一键导入，移动到HDD接口，toPaht 一律改为 必须加 歌手/专辑
2. 加了单元测试用例

############# v0.2.1
写单元测试用例，测试这4个功能（名字重新格式化,移动到HDD, 规整进数据库, 一键导入），数据库 用tp_music_unit_test. 
要求，
1. 验证 song表:个数对不对，歌词有无乱码，nas路径对不对 是不是 歌手/专辑名/歌名， 
2. 验证 singer表，name里是否有多个人
3. 验证 专辑表 
4. 验证 song_singer对应关系

############# v0.2.2
新增接口前提交


############# v0.2.3
新增接口: 一键导入-单歌手-所有专辑
音乐工具，再加一个tab: 一键导入-单歌手-所有专辑。参考：一键导入-单专辑 tab逻辑。具体需求：
1. 源文件目录，只填到作者。
2. 目标路径，也只填到作者。
3. 后台实现逻辑(前端不用管，只要传对参数即可)：
还是分3步走，参考：一键导入-单歌手-所有专辑
注意内容：
- 自动识别源文件目录，下，所有文件夹，每个文件夹是专辑名
- 移动到 目标路径(作者文件夹下)
- 一个一个专辑操作，就类似 循环调用 “一键导入-单歌手-所有专辑” 的接口
4. 一键导入-单歌手-所有专辑功能,统一后台接口：/import/singer/albums

############# v0.2.4
修复bug: 
1. 林俊杰、洪俊扬 , L,O,V-E.mp3 这种格式化的时候，这种识别不了 √
2. 插入song表里的name 都带着后缀名，不应该带 -》 重新导入数据库 -》插入song表里的name 都带着后缀名，不应该带。你应该能连上数据库，我重新导入库后，你修改下
3. song表的name,不能识别 格式化之前的，和之后的 √

############# v0.2.5
修复bug: 
1. 数据库一个专辑对应多个歌手，改为 用前端传参 /singer -> 用这个名字

############# v0.2.6
修复bug: 
1. /imiport/singer/albums 返回给前端，成功个数，加上了歌词，不对。
2. /song/singer/detail?singerId=1 这个接口，查出专辑，没歌曲数据

############# v0.2.7
需求：
1. 没有专辑的歌手不显示，没专辑就表示没有歌
2. sql 剔除 专辑错误关联的歌手

############# v0.2.8
需求：
1. 歌手信息，用AI生成，具体到中国安徽、中国西安，这种 -> 后端实现 √
2. 歌手、专辑图片，找网络上的图片，存到数据库里，不费自己的流量.目前用的qq临时cdn,后期考虑用  √

############# v0.2.9
需求：
1. 歌曲列表，时间显示

############# v0.2.10
修bug：专辑界面，总是随机显示，查db order by 专辑id就好了

############# v0.2.11
修bug: 清除song无效索引。
龙卷风，比如。song没做唯一索引。导致龙卷风应该是jay的，变成邓紫棋的了 -> 后端处理
	- 为什么索引有那么多 name_x?
	- 应该 album_id 和 name组合成唯一索引。现在是这样吗？


############# v0.2.12
把配置db: 改成 tp-music -> music

############# v0.2.13
优化：
1. 生成自动打包脚本 (区分win/linux系统, 可以打包整个项目，或者单个程序)
2. 生成linux 服务
	
############# v0.2.14
优化：
1. 后端获取歌曲文件，能读nas ,能读http/https请求，能读云服务本地文件
2. 查歌手专辑，应该根据专辑id，倒叙排列，最新的在最上面

############# v0.2.15
优化：新增 轮播图接口 banner/getAllBanner

############# v0.2.16
优化：去除song表歌词有类似MD5无用数据，并修改代码

############# v0.2.17
新增：增加aws s3 文件夹、单文件、多文件 增删改查接口

############# v0.2.18
新增：后端登录接口实现，密码改成加密

############# v0.2.19
song表加冗余列 songer_id 前提交

############# v0.2.20
优化：打算重新插入数据库，代码修改好了

############# v0.2.21
修复bug: 有的时候导入不了歌词

############# v0.2.22
修复bug: song.full_name_singer 邓紫棋出现了2次


############# v0.2.23
新增：排行榜相关数据库表，website查询接口请求


############# v0.2.24
优化：生成一些表专门给 排行榜用

############# v0.2.25
优化：酷狗top100 能导入1部分，还有报错，待修复


----------------------删除，给client实现后就删除

文件	说明
models/request.go	添加 RankImportRequest、RankSongItem（含 folderPath）
models/rank.go	添加 album_id 字段
mapper/rank_mapper.go	Rank Mapper
mapper/song_rank_mapper.go	SongRank Mapper
service/rank_service.go	核心逻辑：读取目录→匹配文件→入库
controller/rank_controller.go	RankController
routes/routes.go	添加路由
接口：

POST /rank/import - 导入排行榜数据
GET /rank/list?websiteId=2 - 获取榜单列表
GET /rank/detail?websiteId=2&rankName=top500 - 获取榜单详情
----------------------删除






############# v0.2.14  ？？？？？？？？？？？？？？？？？？？？？没弄
优化： 
1. 打算 根据数据里is_hot 参数，只要是true，就从云服务本地资源找。用device中 name=cloud-local-res 的 url_prefix 拼接

------------------------------------------------------------------ 待实现？？？？？？？
1. 考虑图片用外网图片，不用本地，这样不用占自己的数据库，内存
2. 想出一个 歌手图片，专辑图片，一直能访问的 实现方式

############# sql相关
1. resource/sql/tp_music.sql_v0.1 原项目自带
2. resource/sql/tp_music.sql_v0.2 修改后，song 和 singer 增加关联关系