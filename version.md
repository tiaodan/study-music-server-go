############# v0.1
-- 跑通一个产品(本地) v0.1 -> 原java项目，转成go语言, 上传, 备份
1. 把原项目: https://github.com/tiaodan/study-music-server-java.git


############# v0.2
-- 跑通一个产品(本地) v0.2 -> 把下载的歌曲, 存sql，归整到HDD(硬盘)

目的：把下载的歌曲，根据 歌手-专辑，把名字、歌词名字重新格式化，规整进数据库，并移动到HDD
仅后端实现相关接口
需求拆分：
1. 名字重新格式化。
	- 实现：api指定 歌手-专辑路径，自动遍历文件(mp3/wav/layic)名字,重新格式化(多作者-歌名.文件类型)
2. 移动到HDD
	- 实现：api指定路径，from - to ,根据路径，移动；移动后，查询是否移动成功
3. 规整进数据库
	- 实现：api指定路径，path:xx, 自动遍历文件(mp3/wav/layic)名字,
		-- 插入作者。如果有新作者，插入数据库
		-- 把歌曲名，插入song表
		-- 关联song 和 singer关系

重新设计数据库 （保证数据正确性），
sing/song表应该是有关联关系的，关联时删，不能删，修改时，同时改

############# v0.2.1
优化：
1. 一键导入，移动到HDD接口，toPaht 一律改为 必须加 歌手/专辑
2. 加了单元测试用例

############# v0.2.1
写单元测试用例，测试这4个功能（名字重新格式化,移动到HDD, 规整进数据库, 一键导入），数据库 用tp_music_unit_test. 
要求，
1. 验证 song表:个数对不对，歌词有无乱码，nas路径对不对 是不是 歌手/专辑名/歌名， 
2. 验证 singer表，name里是否有多个人
3. 验证 专辑表 
4. 验证 song_singer对应关系

############# v0.2.2
新增接口前提交


############# v0.2.3
新增接口: 一键导入-单歌手-所有专辑
音乐工具，再加一个tab: 一键导入-单歌手-所有专辑。参考：一键导入-单专辑 tab逻辑。具体需求：
1. 源文件目录，只填到作者。
2. 目标路径，也只填到作者。
3. 后台实现逻辑(前端不用管，只要传对参数即可)：
还是分3步走，参考：一键导入-单歌手-所有专辑
注意内容：
- 自动识别源文件目录，下，所有文件夹，每个文件夹是专辑名
- 移动到 目标路径(作者文件夹下)
- 一个一个专辑操作，就类似 循环调用 “一键导入-单歌手-所有专辑” 的接口
4. 一键导入-单歌手-所有专辑功能,统一后台接口：/import/singer/albums

############# v0.2.4
修复bug: 
1. 林俊杰、洪俊扬 , L,O,V-E.mp3 这种格式化的时候，这种识别不了 √
2. 插入song表里的name 都带着后缀名，不应该带 -》 重新导入数据库 -》插入song表里的name 都带着后缀名，不应该带。你应该能连上数据库，我重新导入库后，你修改下
3. song表的name,不能识别 格式化之前的，和之后的 √

############# v0.2.5
修复bug: 
1. 数据库一个专辑对应多个歌手，改为 用前端传参 /singer -> 用这个名字

############# v0.2.6
修复bug: 
1. /imiport/singer/albums 返回给前端，成功个数，加上了歌词，不对。
2. /song/singer/detail?singerId=1 这个接口，查出专辑，没歌曲数据

############# v0.2.7
需求：
1. 没有专辑的歌手不显示，没专辑就表示没有歌
2. sql 剔除 专辑错误关联的歌手

############# v0.2.8
需求：
1. 歌手信息，用AI生成，具体到中国安徽、中国西安，这种 -> 后端实现 √
2. 歌手、专辑图片，找网络上的图片，存到数据库里，不费自己的流量.目前用的qq临时cdn,后期考虑用  √


------------------------------------------------------------------ 待实现？？？？？？？
1. 考虑图片用外网图片，不用本地，这样不用占自己的数据库，内存

############# sql相关
1. resource/sql/tp_music.sql_v0.1 原项目自带
2. resource/sql/tp_music.sql_v0.2 修改后，song 和 singer 增加关联关系